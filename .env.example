# Vercel / Google Play purchase verification
# Create a Google Cloud service account with Google Play Android Publisher access,
# then store the downloaded JSON as this single environment variable in Vercel.
GOOGLE_PLAY_SERVICE_ACCOUNT_JSON={"type":"service_account"}
