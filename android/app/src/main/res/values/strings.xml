<resources>
    <!-- Production: supply the trusted HTTPS purchase-verifier URL through the release configuration process. -->
    <!-- Deliberately empty in source control so builds fail closed until verification is configured. -->
    <string name="purchase_verification_url"></string>
</resources>
