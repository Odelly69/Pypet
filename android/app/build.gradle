plugins {
    id 'com.android.application'
    id 'com.chaquo.python'
}

android {
    namespace 'com.odelly.pypet'
    compileSdk 35

    defaultConfig {
        applicationId 'com.odelly.pypet'
        minSdk 24
        targetSdk 35
        versionCode 6
        versionName '0.1.0-beta5'

        // Chaquopy requires explicit native ABIs.
        ndk {
            abiFilters 'arm64-v8a', 'x86_64'
        }

        // Release verification endpoint is supplied by the release environment.
        // Debug builds intentionally default to empty and fail closed for purchases.
        buildConfigField 'String', 'PURCHASE_VERIFICATION_URL', '"' + (project.findProperty('pypetPurchaseVerifyUrl') ?: '') + '"'
    }

    buildTypes {
        debug {
            buildConfigField 'String', 'REWARDED_AD_UNIT_ID', '"ca-app-pub-3940256099942544/5224354917"'
        }
        release {
            minifyEnabled false
            buildConfigField 'String', 'REWARDED_AD_UNIT_ID', '"ca-app-pub-3352973554477677/6771650805"'
        }
    }
}

chaquopy {
    defaultConfig {
        version = '3.13'
    }
}

dependencies {
    implementation 'com.google.android.gms:play-services-ads:25.4.0'
    implementation 'com.android.billingclient:billing:9.1.0'
}
